using _Scripts.Bubbles;
using UnityEngine;

public class TouchManager : MonoBehaviour
{
    private Bubble[] _selectedBubbles;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
